{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>FirstNameArtist</th>
                <td>{{ artist.firstNameArtist }}</td>
            </tr>
            <tr>
                <th>LastNameArtist</th>
                <td>{{ artist.lastNameArtist }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ artist.job }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artist_index') }}">back to list</a>

    <a href="{{ path('artist_edit', {'id': artist.id}) }}">edit</a>

    {{ include('artist/_delete_form.html.twig') }}
{% endblock %}
