{% extends 'base.html.twig' %}

{% block title %}Hello ConcertController!{% endblock %}

{% block body %}
<style>
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }


    h2 {
        padding-bottom: 10px;
    }
</style>
    <h2>Liste des futurs concerts en concert : </h2>

    {% if isNull %}
        <h1>Alo?</h1>
    {% endif %}

    {% if isNull == false %}
        {% for c in theConcert %}
            <ul>
                <a href="display/{{ c.id }}"> {{ c.nameConcert}} </a> <a href="delete/{{ c.id }}"> SUPPRIMER LE CONCERT </a>
            </ul>
        {% endfor %}
    {% endif %}

    <div>
        <a href="{{ path('concert_create') }}">
            Créer un concert
        </a>
    </div>
{% endblock %}
